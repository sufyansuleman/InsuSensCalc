name: R-CMD-build

on:
  push:
    branches:
      - main
  release:
    types:
      - published

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup R
        uses: r-lib/actions/setup-r@v2

      - name: Install build dependencies
        run: |
          Rscript --vanilla -e 'options(repos = c(CRAN = "https://cloud.r-project.org")); if (!requireNamespace("remotes", quietly = TRUE)) install.packages("remotes")'
          Rscript --vanilla -e 'options(repos = c(CRAN = "https://cloud.r-project.org")); remotes::install_deps(dependencies = FALSE)'

      - name: Build package tarball
        run: R CMD build .

      - name: Upload source package artifact
        uses: actions/upload-artifact@v4
        with:
          name: InsuSensCalc-source
          path: InsuSensCalc_*.tar.gz
